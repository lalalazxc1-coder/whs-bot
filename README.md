# 📦 Kulan Oil Warehouse Bot

Telegram-бот для автоматизации складской логистики, сбора отчетов по остаткам, заказов материалов и обработки обращений сотрудников.

## 🚀 Функционал

### 👤 Для сотрудников (Пользователей)
*   **Авторизация:** Выбор языка (RU/KZ) и филиала при первом входе. Приветствие по имени.
*   **📦 Отправить остатки:** Пошаговая форма (можно начать по кнопке из напоминания от админа).
*   **📦 Заказ материалов:** Удобная корзина для заказа расходников (выбор из списка).
*   **⚠️ Сообщить о проблеме:** Отправка жалоб и ошибок (падает в группу Поддержки).
*   **❓ Задать вопрос:** Задать вопрос по логистике (падает в группу Вопросов).
*   **📞 Контакты:** Быстрый доступ к номерам телефонов отделов.
*   **⚙️ Настройки:** Смена языка и филиала в любое время.

### 👷‍♂️ Для Администраторов и Логистов
*   **Тикет-система:** Все обращения попадают в специальные группы Telegram (Поддержка или Вопросы).
    *   Создается тикет с уникальным ID.
    *   Кнопка **«Ответить»** позволяет отправить сообщение пользователю прямо из группы.
    *   История переписки сохраняется.
*   **Управление данными:**
    *   Управление списком контактов через бота (Добавить/Удалить).
    *   Добавление филиалов и товаров через команды.
*   **📢 Рассылка:** Отправка объявлений всем сотрудникам или конкретному филиалу.
*   **📊 Аналитика:** Выгрузка отчетов (Инвентаризация, Заказы, Тикеты) в Excel за разные периоды.

---

## 🛠 Команды Бота

### 🔹 Основные
*   `/start` — Запуск бота / Главное меню.
*   `/help` — Справка.

### 🔹 Администрирование (в личке с ботом)
*Доступно только пользователям из списка `ADMIN_IDS` в .env*

*   `/admin` — **Админ-панель** (Рассылка, Excel, Контакты).
*   `/remind <Дата/Время>` — **Напоминание об отчете**.
    *   Отправляет всем пользователям сообщение с датой и кнопкой "Отправить остатки".
    *   *Пример: `/remind Завтра до 12:00`*
*   `/add_branch <Название>` — Добавить новый филиал.
*   `/add_item <Название>` — Добавить товар.
*   `/contacts_admin` — Список контактов с ID (для удаления вручную).
*   `/add_contact <Отдел> <Инфо>` — Добавить контакт вручную.
    *   *Пример: `/add_contact Логистика Иван 87771234567 email@test.kz`*
*   `/del_contact <ID>` — Удалить контакт вручную.

### 🔹 Работа в Группе Поддержки
*   `/tickets` — Показать список всех открытых заявок.
*   `/report` — Показать последние 5 отчетов текстом.

---

## ⚙️ Установка и Запуск

### 1. Требования
*   Python 3.10+
*   Библиотеки из `requirements.txt`

### 2. Установка
```bash
# Клонировать репозиторий (или скачать файлы)
git clone ...

# Создать виртуальное окружение
python -m venv venv
# Активировать (Windows)
.\venv\Scripts\activate
# Активировать (Mac/Linux)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 3. Настройка (.env)
Переименуйте `.env.example` в `.env` и укажите данные:
```ini
BOT_TOKEN=123456:Ваш_Токен
ADMIN_IDS=111111,222222         # ID администраторов через запятую
SUPPORT_GROUP_ID=-100xxxxxxxxx  # ID группы для проблем/жалоб
QUESTIONS_GROUP_ID=-100yyyyyyy  # ID группы для вопросов
```

### 4. Запуск
```bash
python main.py
```
*При первом запуске создастся база данных `warehouse.db` автоматически.*

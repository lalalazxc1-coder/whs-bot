# 📦 Kulan Oil Warehouse Bot

Telegram-бот для автоматизации складской логистики, сбора отчетов по остаткам, заказов материалов и обработки обращений сотрудников.

## 🚀 Функционал

### 👤 Для сотрудников (Пользователей)
*   **Авторизация:** Выбор языка (RU/KZ) и филиала при первом входе.
*   **📦 Отправить остатки:** Пошаговая форма для подачи отчета по инвентаризации.
*   **🛒 Заказ материалов:** Удобная корзина для заказа расходников (выбор из списка).
*   **🆘 Обращение в логистику:** Отправка текстовых сообщений, фото или видео напрямую в отдел поддержки (создается Тикет).
*   **📞 Контакты:** Быстрый доступ к номерам телефонов отделов.
*   **⚙️ Настройки:** Смена языка и филиала в любое время.

### 👷‍♂️ Для Администраторов и Логистов
*   **Тикет-система:** Все обращения попадают в специальную группу Telegram.
    *   Создается тикет с уникальным ID.
    *   Кнопка **«Ответить»** позволяет отправить сообщение пользователю прямо из группы.
    *   История переписки сохраняется.
*   **Управление данными:**
    *   Добавление филиалов и товаров через команды.
*   **📢 Рассылка:** Отправка объявлений всем сотрудникам или конкретному филиалу.
*   **📊 Аналитика:** Выгрузка полного отчета (Инвентаризация + Тикеты + Переписка) в Excel.

---

## 🛠 Команды Бота

### 🔹 Основные
*   `/start` — Запуск бота / Главное меню.

### 🔹 Администрирование (в личке с ботом)
*Доступно только пользователям из списка `ADMIN_IDS` в .env*

*   `/admin` — **Админ-панель** (Рассылка, Выгрузка Excel).
*   `/add_branch <Название>` — Добавить новый филиал.
    *   *Пример: `/add_branch Алматы-1`*
*   `/add_item <Название>` — Добавить товар (для инвентаризации и заказа).
    *   *Пример: `/add_item Скотч`*
*   `/admin_help` — Справка по командам.

### 🔹 Работа в Группе Поддержки
*   `/tickets` — Показать список всех открытых заявок (тикетов).
*   `/report` — Показать последние 5 отчетов по остаткам.

---

## ⚙️ Установка и Запуск

### 1. Требования
*   Python 3.10+
*   Библиотеки из `requirements.txt`

### 2. Установка
```bash
# Клонировать репозиторий (или скачать файлы)
git clone ...

# Создать виртуальное окружение
python -m venv venv
# Активировать (Windows)
.\venv\Scripts\activate
# Активировать (Mac/Linux)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 3. Настройка (.env)
Переименуйте `.env.example` в `.env` и укажите данные:
```ini
BOT_TOKEN=123456:Ваш_Токен
ADMIN_IDS=111111,222222         # ID администраторов через запятую
SUPPORT_GROUP_ID=-100xxxxxxxxx  # ID супергруппы для тикетов
```

### 4. Запуск
```bash
python main.py
```
*При первом запуске создастся база данных `warehouse.db` автоматически.*
